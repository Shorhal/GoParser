package main

/* import (
	"context"
	"fmt"
	"log"
	"strconv"
	"strings"
	"time"

	"github.com/chromedp/cdproto/emulation"
	"github.com/chromedp/chromedp"
)

func OrgHtmlget(url string, ctx context.Context) string {

	var orgHtml string

	if err := chromedp.Run(ctx,
		chromedp.OuterHTML(`#datagrid > div > div.dx-datagrid-rowsview.dx-scrollable.dx-visibility-change-handler.dx-scrollable-both.dx-scrollable-simulated.dx-scrollable-customizable-scrollbars > div > div > div.dx-scrollable-content > div > table`, &dataGrid, chromedp.ByID),
	); err != nil {

		log.Fatal(err)
	}
	return orgHtml
}

func ProdHtmlGet(url string, ctx context.Context, numberOfPages int) string {

	var prodHtml string

	if err := chromedp.Run(ctx,
		chromedp.OuterHTML(`#datagrid > div > div.dx-datagrid-rowsview.dx-scrollable.dx-visibility-change-handler.dx-scrollable-both.dx-scrollable-simulated.dx-scrollable-customizable-scrollbars > div > div > div.dx-scrollable-content > div > table`, &dataGrid, chromedp.ByID),
	); err != nil {

		log.Fatal(err)
	}
	return prodHtml
}	*/
